package com.iot.project.cartrackingapp.utility.constants.enums;

// Enum to hold differen alert levels. 
// If there is a new level which may be added in the future, 
// we need to add it here and use it wherever needed
public enum AlertLevel {
	NONE, HIGH, MEDIUM, LOW;
}
